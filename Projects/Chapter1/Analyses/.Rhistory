count_table_no_contam <- count_table_no_contam[,-c(true_index)]  #remove those samples
# Make new taxonomy table
# set up a logical vector for rows which are contamination
vector_for_contam_taxonomy <- taxonomy$`Feature ID` %in% contam_asvs
#remove contaminating ASVs
taxonomy_no_contam <- taxonomy[!vector_for_contam_taxonomy,]
# Make a new tree file
# This is a phyloseq object so we can use a phyloseq command called prune_taxa
tree_no_contam <- prune_taxa(count_table_no_contam$`#OTU ID`, tree)
# Use rarecurve from the vegan package. Rarecurve expects teh datasetas a dataframe so we need to use as.data.frame again:
count_table_no_contam_df <- ad.data.frame(count_table_no_contam)
# Use rarecurve from the vegan package. Rarecurve expects teh datasetas a dataframe so we need to use as.data.frame again:
count_table_no_contam_df <- as.data.frame(count_table_no_contam)
#Plot the rarefaction curves, color-coding by the colors listed in sample_info_tab, which indicate sample type, and tranforming using t()
rarecurve(t(count_table_no_contam_df), step=100, cex=0.5, col=sample_info_tab$Run, lwd=2, ylab="ASVs", label=T)
#Plot the rarefaction curves, color-coding by the colors listed in sample_info_tab, which indicate sample type, and tranforming using t()
rarecurve(t(count_table_no_contam_df), step=100, cex=0.5, lwd=2, ylab="ASVs", label=T)
rarecurve?
()
?rarecurve
palette(rainbow(nrow(sample_info_tab)))
x <- palette(rainbow(nrow(sample_info_tab)))
#Plot the rarefaction curves, color-coding by the colors listed in sample_info_tab, which indicate sample type, and tranforming using t()
rarecurve(t(count_table_no_contam_df), step=100, col=palette(rainbow(nrow(sample_info_tab))), cex=0.5, lwd=2, ylab="ASVs", label=T)
library(RColorBrewer)
rarecurve(t(count_table_no_contam_df), step=100, col= brewer.pal(nrow(sample_info_tab), "BrBG"), cex=0.5, lwd=2, ylab="ASVs", label=T)
# And add a vertical line to the plot indicating the fewest # of sequences from any sample
abline(v=(min(rowSums(t(count_table_no_contam_df)))))
#Plot the rarefaction curves, color-coding by the colors listed in sample_info_tab, which indicate sample type, and tranforming using t()
rarecurve(t(count_table_no_contam_df), step=100, col=palette(rainbow(nrow(sample_info_tab))), cex=0.5, lwd=2, ylab="ASVs", label=T)
# And add a vertical line to the plot indicating the fewest # of sequences from any sample
abline(v=(min(rowSums(t(count_table_no_contam_df)))))
abline(v=(min(rowSums(t(count_table_no_contam_df)))))
rarecurve(t(count_table_no_contam_df), step=100, cex=0.5, col=palette(rainbow(nrow(sample_info_tab))), lwd=2, ylab="ASVs", label=T)
# And add a veritical line to the plot indicating the fewest # of sequences from any sample
abline(v=(min(rowSums(t(count_table_no_contam_df)))))
min(rowSums(t(count_table_no_contam_df)))
rarecurve(t(count_table_no_contam_df), step=100, cex=0.5, col=palette(rainbow(nrow(sample_info_tab))), lwd=2, ylab="ASVs", label=T) +
# And add a veritical line to the plot indicating the fewest # of sequences from any sample
abline(v=(min(rowSums(t(count_table_no_contam_df)))))
# Use rarecurve from the vegan package. Rarecurve expects teh datasetas a dataframe so we need to use as.data.frame again:
count_table_no_contam_df <- as.data.frame(count_table_no_contam)
#Plot the rarefaction curves, color-coding by the colors listed in sample_info_tab, which indicate sample type, and tranforming using t()
rarecurve(t(count_table_no_contam_df), step=100, cex=0.5, col=palette(rainbow(nrow(sample_info_tab))), lwd=2, ylab="ASVs", label=T)
# And add a veritical line to the plot indicating the fewest # of sequences from any sample
abline(v=(min(rowSums(t(count_table_no_contam_df)))))
count_table_no_contam_no_singletons <- filter(count_table_no_contam, rowSums(count_table_no_contam)>1)
View(contam_df)
View(count_table_no_contam_no_singletons)
count_table_no_contam_no_singletons_df <- count_table_no_contam_df[which(!rowSums(count_table_no_contam_df) <= 1), ]
# First convert to phyloseq package
count_table_no_contam_phyloseq <- otu_table(count_table_no_contam, taxa_are_rows = TRUE)
# Then filter using filter taxa
count_table_no_contam_no_singletons_phyloseq <- prune_taxa(taxa_sims(count_table_no_contam_phyloseq) > 1, count_table_no_contam_phyloseq)
# Then filter using filter taxa
count_table_no_contam_no_singletons_phyloseq <- prune_taxa(taxa_sums(count_table_no_contam_phyloseq) > 1, count_table_no_contam_phyloseq)
count_table_hellinger <- decostand(t(count_table_no_contam_no_singletons), "hellinger")
# Convert back to a tibble and switch rows for columns again (as_tibble has a weird thing where it will delete the rownames unless you specify rownames = NA)
count_table_hellinger <- as_tibble(t(count_table_hellinger), rownames = NA)
# Compare the untransformed to the transformed data
head(count_table_no_contam_no_singletons) #untransformed
head(count_table_hellinger)
### Make a results directly
dir.create("results)
#dir.create("results/cleaned_files)
### Make a results directly
# And export all files:
#fasta
write.fasta(asv_fasta_no_contam, taxonomy_no_contam$`Feature ID`, "results/cleaned_files/Sommers_16S_ASVs.fasta")
#transformed count table
write.table(count_table_hellinger, "results/cleaned_files/count_table_hellinger.tsv", sep="\t", quote=F, col.names=NA)
#taxonomy
write.table(taxonomy_no_contam, "results/cleaned_files/taxonomy.tsv," sep="\t", quote=F, col.names=NA)
#taxonomy
write.table(taxonomy_no_contam, "results/cleaned_files/taxonomy.tsv", sep="\t", quote=F, col.names=NA)
# tree file
ape::write.tree(tree_no_contam, "results/cleaned_files/tree.nwk")
save(asv_fasta_no_contam, count_table_hellinger, taxonomy_no_contam, tree_no_contam, file = "results/cleaned_files/cleanedfiles.RData")
load("results/cleaned_files/cleanedfiles.RData")
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(phyloseq)
library(ape)
rm(list=ls())
Install and load packages
```{r}
library(tidyverse)
library(phyloseq)
library(ape)
```
graphics.off()
rm(list=ls())
library(tidyverse)
library(phyloseq)
library(ape)
load("results/cleaned_files/cleanedfiles.RData")
sample_info_tab <- read_tsv("export/SraRunTable-wrangled.tsv")
sample_info_tab <- read_tsv("export/SraRunTable-wrangle.tsv")
sample_info_tab <- read_csv("export/SraRunTable-wrangle.tsv")
View(sample_info_tab)
sample_info_tab <- sample_info_tab[-c(1),]
sample_info_tab <- sample_info_tab[,-c(1)]
View(sample_info_tab)
head(taxonomy_no_contam)
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\_", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
seperate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\_", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
separate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
View(taxonomy_no_contam)
View(taxonomy)
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\__", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
separate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
View(taxonomy)
View(taxonomy)
ASV = out_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
ASV = otu_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META = sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
head(taxa_names(TAX))
head(sample_names(ASV))
head(sample_names(META))
View(sample_info_tab)
count_table_hellinger
ASV
ASV = otu_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META = sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
META = sample_data(data.frame(sample_info_tab))
View(META)
META	=	sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$`sample-id`))
META	=	sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
unique(sample_info_tab)
unique(sample_info_tab$Run)
length(unique(sample_info_tab$Run))
sample_info_tab <- read_csv("export/SraRunTable-wrangle.tsv")
#delete unneccessary row and column
sample_info_tab <- sample_info_tab[-c(1),]
sample_info_tab <- sample_info_tab[,-c(1)]
sample_info_tab <- sample_info_tab[!grepl("18S", sample_info_tab$Library.Name),] #only include 16S samples
sample_info_tab <- sample_info_tab[-c(42:80),] #remove reverse reads so we only have one entry per sample
#delete unneccessary row and column
sample_info_tab <- sample_info_tab[-c(1),]
sample_info_tab <- sample_info_tab[,-c(1)]
sample_info_tab <- sample_info_tab[!grepl("18S", sample_info_tab$Library.Name),] #only include 16S samples
sample_info_tab <- sample_info_tab[-c(42:80),] #remove reverse reads so we only have one entry per sample
The taxonomy in the taxonomy table is retained in one column and the different levels are deparated by underscores, eg:
```{r}
head(taxonomy_no_contam)
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\__", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
separate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
head(taxonomy)
ASV = otu_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
View(sample_info_tab)
sample_info_tab <- read_csv("export/SraRunTable-wrangle.tsv")
#delete unneccessary row and column
sample_info_tab <- sample_info_tab[-c(1),]
View(sample_info_tab)
sample_info_tab <- sample_info_tab[,-c(1)]
View(sample_info_tab)
sample_info_tab <- sample_info_tab[!grepl("18S", sample_info_tab$Library.Name),] #only include 16S samples
View(sample_info_tab)
sample_info_tab <- sample_info_tab[-c(42:80),] #remove reverse reads so we only have one entry per sample
View(sample_info_tab)
head(taxonomy_no_contam)
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\__", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
separate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
head(taxonomy)
View(sample_info_tab)
ASV = otu_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
View(META)
head(taxa_names(TAX))
head(taxa_names(ASV))
head(taxa_names(tree_no_contam))
head(sample_names(ASV))
head(sample_names(META))
View(count_table_hellinger)
sample_info_tab_1 <- read_csv("export/SraRunTable-wrangle.tsv")
View(sample_info_tab_1)
View(sample_info_tab)
x <- sample_info_table[-cstartsWith(sample_info_tab$Library.Name, "NT")]
x <- sample_info_tab[-cstartsWith(sample_info_tab$Library.Name, "NT")]
x <- sample_info_tab[-c(startsWith(sample_info_tab$Library.Name, "NT"))]
View(x)
x <- sample_info_tab[-c(startsWith(sample_info_tab$Library.Name, "NT")),]
View(x)
#remove blanks
x <- startsWith(sample_info_tab$Library.Name, "NT")
sample_info_tab_1 <- read_csv("export/SraRunTable-wrangle.tsv")
#delete unneccessary row and column
sample_info_tab <- sample_info_tab[-c(1),]
sample_info_tab <- sample_info_tab[,-c(1)]
sample_info_tab <- sample_info_tab[!grepl("18S", sample_info_tab$Library.Name),] #only include 16S samples
sample_info_tab <- sample_info_tab[-c(42:80),] #remove reverse reads so we only have one entry per sample
#remove blanks
x <- startsWith(sample_info_tab$Library.Name, "NT")
true_index <- NULL
for (i in 1:length(x)){
if (x[[i]] == "TRUE"){
true_index <- c(true_index, i)
}
}
sample_info_tab <- sample_info_tab[,-c(true_index)]  #remove those samples
View(sample_info_tab)
sample_info_tab <- sample_info_tab[-c(true_index),]  #remove those samples
View(sample_info_tab)
sample_info_tab <- read_csv("export/SraRunTable-wrangle.tsv")
#delete unneccessary row and column
sample_info_tab <- sample_info_tab[-c(1),]
sample_info_tab <- sample_info_tab[,-c(1)]
sample_info_tab <- sample_info_tab[!grepl("18S", sample_info_tab$Library.Name),] #only include 16S samples
sample_info_tab <- sample_info_tab[-c(42:80),] #remove reverse reads so we only have one entry per sample
#remove blanks
x <- startsWith(sample_info_tab$Library.Name, "NT")
true_index <- NULL
for (i in 1:length(x)){
if (x[[i]] == "TRUE"){
true_index <- c(true_index, i)
}
}
sample_info_tab <- sample_info_tab[-c(true_index),]  #remove those samples
View(sample_info_tab)
head(taxonomy_no_contam)
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\__", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
separate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
head(taxonomy)
ASV = otu_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
head(taxa_names(TAX))
head(taxa_names(ASV))
head(taxa_names(tree_no_contam))
head(sample_names(ASV))
head(sample_names(META))
head(taxa_names(TAX))
head(taxa_names(ASV))
head(taxa_names(tree_no_contam))
ps <- phyloseq(ASV, TAX, META, tree_no_contam)
rank_names(ps)
unique(tax_table(ps)[, "Kingdom"])
table(tax_table(ps)[, "Kingdom"], exclude = NULL)
ps <- subset_taxa(ps, !is.na(Kingdom) & !Kingdom %in% c("Unassigned", "Eukaryota"))
table(tax_table(ps)[, "Kingdom"], exclude = NULL)
table(tax_table(ps)[, "Phylum"], exclude = NULL)
ps <- subset_taxa(ps, !is.na(Phylum))
ps <- subset_taxa(ps, !is.na(Phylum) & Phylum <= 2)
ps
#get names of phyla with abundances of 2 or less
table(tax_table(ps)[, "Phylum"], exclude = NULL)
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)
x
class(x)
View(x)
x$Var
x$Var1
x[x <= 10]
x[x <= 2]
x[x > 2]
#get names of phyla with abundances of 2 or less
table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2]
ps <- subset_taxa(ps, !is.na(Phylum) ps > 2)
ps <- subset_taxa(ps, !is.na(Phylum) $ ps > 2)
ps <- subset_taxa(ps, !is.na(Phylum) & ps > 2)
ps
#get names of phyla with abundances of 2 or less
table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2]
#get names of phyla with abundances of 2 or less
table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2]
name(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])
row.name(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])
View(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])
(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])[1]
(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])[1,]
(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])[,1]
(table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2])
#get names of phyla with abundances of 2 or less
table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) > 2]
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 2]
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 2]
ps <- subset_taxa(ps, !is.na(Phylum) & !Phylum %in% x)
table(tax_table(ps)[, "Phylum"], exclude = NULL)
x
rownames(x)
ps <- subset_taxa(ps, !is.na(Phylum) & !Phylum %in% rownames(x))
table(tax_table(ps)[, "Phylum"], exclude = NULL)
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 3]
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 3]
ps <- subset_taxa(ps, !is.na(Phylum) & !Phylum %in% rownames(x))
table(tax_table(ps)[, "Phylum"], exclude = NULL)
x
ps <- subset_taxa(ps, !is.na(Phylum) & !Phylum %in% rownames(x))
table(tax_table(ps)[, "Phylum"], exclude = NULL)
rownames(x)
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 3]
x
rownames(x)
View(x)
ps <- phyloseq(ASV, TAX, META, tree_no_contam)
rank_names(ps)
unique(tax_table(ps)[, "Kingdom"])
table(tax_table(ps)[, "Kingdom"], exclude = NULL)
ps <- subset_taxa(ps, !is.na(Kingdom) & !Kingdom %in% c("Unassigned", "Eukaryota"))
table(tax_table(ps)[, "Kingdom"], exclude = NULL)
table(tax_table(ps)[, "Phylum"], exclude = NULL)
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 3]
rownames(x)
ps <- subset_taxa(ps, !is.na(Phylum) & !Phylum %in% rownames(x))
table(tax_table(ps)[, "Phylum"], exclude = NULL)
# First define the function from link above to find furthest outgroup
pick_new_outgroup <- function(tree.unrooted){
require("magrittr")
require("data.table")
require("ape") #appe::Ntip
#tablif parts of the tree that we need
treeDT <-
cbind(
data.table(tree.unrooted$edge),
data.table(length = tree.unrooted$edge.length)
)[1:Ntip(tree.unrooted)] %>%
cbind(data.table(id = tree.unrooted$tip.label))
#Take out the longest terminal branch as outgroup
new.outgroup <- treeDT[which.max(length)]$id
return(net.outgroup)
}
# First define the function from link above to find furthest outgroup
pick_new_outgroup <- function(tree.unrooted){
require("magrittr")
require("data.table")
require("ape") #appe::Ntip
#tablif parts of the tree that we need
treeDT <-
cbind(
data.table(tree.unrooted$edge),
data.table(length = tree.unrooted$edge.length)
)[1:Ntip(tree.unrooted)] %>%
cbind(data.table(id = tree.unrooted$tip.label))
#Take out the longest terminal branch as outgroup
new.outgroup <- treeDT[which.max(length)]$id
return(new.outgroup)
}
# Then run on my phyloseq tree
my.tree <- phy_tree(tree_no_contam)
out.group <- pick_new_outgroup(my.tree)
out_group
out.group
# Then use this outgroup to root the tree
new.tree1 <- ape::root(my.tree, outgroup=out.group, resolve.root=TRUE)
# And convert to dichotomy tree
new.tree2 <- ape::multi2di(new.tree1)
phy_tree(ps) <- new.tree2
phy_tree(ps)
# First aglomerate the ASVs at the phylum level using the phyloseq function, tax_glom
phylumGlommed = tax_glom(ps, "Phylum")
# and plot
plot_bar(phylumGlommed, x = "Sample", fill = "Phylum")
ps_dist <- distance(ps, method = "bray")
ps_dist
out.pcoa <- ordinate(ps, method = "PCoA", distance = "bray")
pcoa_plot = plot_ordination(ps, out.pcoa, color = "char", shape= "type") +
geom_point(size = 3)
pcoa_plot
View(ps)
View(ps)
View(sample_info_tab)
View(sample_info_tab_1)
View(sample_info_tab)
pcoa_plot = plot_ordination(ps, out.pcoa, color = "Sample.name") +
geom_point(size = 3)
View(sample_info_tab_1)
View(ps)
pcoa_plot = plot_ordination(ps, out.pcoa, color = "Sample.Name") +
geom_point(size = 3)
pcoa_plot
sample_info_tab$Sample.Name[[1]]
#add a column with glacier name
Glacier <- NULL
for (i in 1:nrow(sample_info_tab)){
if (startsWith(sample_info_tab$Sample.Name[[i]], "C")){
Glacier <- c(Glacier, "Canada")
} else (startsWith(sample_info_tab$Sample.Name[[i]], "T")){
#add a column with glacier name
Glacier <- NULL
for (i in 1:nrow(sample_info_tab)){
if (startsWith(sample_info_tab$Sample.Name[[i]], "C")){
Glacier <- c(Glacier, "Canada")
} else {
Glacier <- c(Glacier, "Taylor")
}
}
Glacier
cbind(sample_info_tab, Glacier)
View(sample_info_tab)
sample_info_tab <- cbind(sample_info_tab, Glacier)
View(sample_info_tab_1)
View(sample_info_tab)
head(taxonomy_no_contam)
taxonomy <- taxonomy_no_contam %>%
mutate(taxonomy=str_replace_all(string=Taxon, pattern="D_\\d*\\__", replacement="")) %>%
mutate(taxonomy=str_replace_all(string=taxonomy, pattern=";$", replacement="")) %>%
separate(taxonomy, into=c("Kingdom", "Phylum", "Class", "Order", "Family", "Genus", "Species"), sep=";") %>%
select(-Taxon, -Confidence) %>%
column_to_rownames(var='Feature ID')
head(taxonomy)
ASV = otu_table(data.frame(count_table_hellinger), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(sample_info_tab, row.names = sample_info_tab$Run))
```{r}
head(taxa_names(TAX))
head(taxa_names(ASV))
head(taxa_names(tree_no_contam))
head(sample_names(ASV))
head(sample_names(META))
ps <- phyloseq(ASV, TAX, META, tree_no_contam)
rank_names(ps)
unique(tax_table(ps)[, "Kingdom"])
table(tax_table(ps)[, "Kingdom"], exclude = NULL)
ps <- subset_taxa(ps, !is.na(Kingdom) & !Kingdom %in% c("Unassigned", "Eukaryota"))
table(tax_table(ps)[, "Kingdom"], exclude = NULL)
table(tax_table(ps)[, "Phylum"], exclude = NULL)
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 3]
#get names of phyla with abundances of 2 or less
x <- table(tax_table(ps)[, "Phylum"], exclude = NULL)[table(tax_table(ps)[, "Phylum"], exclude = NULL) < 3]
ps <- subset_taxa(ps, !is.na(Phylum) & !Phylum %in% rownames(x))
table(tax_table(ps)[, "Phylum"], exclude = NULL)
# First define the function from link above to find furthest outgroup
pick_new_outgroup <- function(tree.unrooted){
require("magrittr")
require("data.table")
require("ape") #appe::Ntip
#tablif parts of the tree that we need
treeDT <-
cbind(
data.table(tree.unrooted$edge),
data.table(length = tree.unrooted$edge.length)
)[1:Ntip(tree.unrooted)] %>%
cbind(data.table(id = tree.unrooted$tip.label))
#Take out the longest terminal branch as outgroup
new.outgroup <- treeDT[which.max(length)]$id
return(new.outgroup)
}
# Then run on my phyloseq tree
my.tree <- phy_tree(tree_no_contam)
out.group <- pick_new_outgroup(my.tree)
out.group
# Then use this outgroup to root the tree
new.tree1 <- ape::root(my.tree, outgroup=out.group, resolve.root=TRUE)
# And convert to dichotomy tree
new.tree2 <- ape::multi2di(new.tree1)
phy_tree(ps) <- new.tree2
phy_tree(ps)
# First aglomerate the ASVs at the phylum level using the phyloseq function, tax_glom
phylumGlommed = tax_glom(ps, "Phylum")
# and plot
plot_bar(phylumGlommed, x = "Sample", fill = "Phylum")
ps_dist <- distance(ps, method = "bray")
ps_dist
out.pcoa <- ordinate(ps, method = "PCoA", distance = "bray")
pcoa_plot = plot_ordination(ps, out.pcoa, color = "Glacier") +
geom_point(size = 3)
pcoa_plot
evals <- out.pcoa$values$Eigenvalues
pcoa_plot.scaled = plot_ordination(ps, out.pcoa, color="Glacier")+
geom_point(size=3)+
coord_fixed(sqrt(evals[2]/evals[1]))
pcoa_plot.scaled
out.pcoa <- ordinate(ps, method = "PCoA", distance = "wunifrac")
wuf_pcoa_plot = plot_ordination(ps, out.pcoa, color="Glacier")+
geom_point(size=3)
wuf_pcoa_plot
out.nmds <- ordinate(ps, method = "NMDS", distance = "bray")
nmds_plot = plot_ordination(ps, out.nmds, color="Glacier")+
geom_point(size=3)
nmds_plot
out.nmds <- ordinate(ps, method = "NMDS", distance = "bray")
nmds_plot = plot_ordination(ps, out.nmds, color="Glacier")+
geom_point(size=3)
nmds_plot
