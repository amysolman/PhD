#Check if the tree is rooted
my.ps@phy_tree #Rooted, includes branch lengths
#It is a rooted tree so...
df.pd <- pd(t(asv_tab), my_tree, include.root = TRUE) #find faiths diversity metric for each sample.
#Calculate Pilou evenness
div_pilou <- diversity(t(count_table_clean), index = "simpson")/log(df.pd$SR) #Here we use the species count we got earlier
#bind alpha diversity indices into a dataframe
diversity <- cbind(df.pd, div_shan, div_simp, div_inv_simp, div_pilou)
colnames(diversity) <- c("Faith", "Richness", "Shannon", "Simpson", "Inv Simpson", "Pilou")
my.data <- cbind(metadata, diversity)
#remove columns with data we're not interested in
my.data.sub <- subset(my.data, select = c(1, 10, 16, 19, 34, 36, 48, 50, 51, 52, 53, 54, 55, 56))
rownames(my.data.sub) <- my.data.sub$Run
my.data.sub <- subset(my.data.sub, select = -c(1))
my.data.sub <- as.data.frame(lapply(my.data.sub, function(x) as.numeric(as.character(x))))
my.data.sub$Glacier <- metadata$Glacier
my.data.sub <- my.data.sub[complete.cases(my.data.sub), ]
Chlorophyll <- sample(as.numeric(my.data.sub$Chlorophyll, size = 12))
Chlorophyll <- sample(as.numeric(my.data.sub$Chlorophyll), size = 12)
Depth <- sample(as.numeric(my.data.sub$Depth), size = 12)
DNA_per_g <- sample(as.numeric(my.data.sub$DNA_per_g), size = 12)
org_matter <- sample(as.numeric(my.data.sub$org_matter), size = 12)
pH <- sample(as.numeric(my.data.sub$pH), size = 12)
water_content <- sample(as.numeric(my.data.sub$water_content), size = 12)
area <- sample(as.numeric(my.data.sub$area), size = 12)
Faith <- sample(as.numeric(my.data.sub$Faith), size = 12)
Richness <- sample(as.numeric(my.data.sub$Richness), size = 12)
Shannon <- sample(as.numeric(my.data.sub$Shannon), size = 12)
Simpson <- sample(as.numeric(my.data.sub$Simpson), size = 12)
Inv.Simpson <- sample(as.numeric(my.data.sub$Inv.Simpson), size = 12)
Pilou <- sample(as.numeric(my.data.sub$Pilou), size = 12)
Glacier <- rep("Solman", 12)
df <- cbind(Chlorophyll, Depth, DNA_per_g, org_matter, pH, water_content, area, Faith, Richness, Shannon, Simpson, Inv.Simpson, Pilou, Glacier)
my.data.sub.X <- rbind(my.data.sub, df)
View(my.data.sub.X)
plot(Richness ~ Glacier, data = my.data.sub.X)
class(my.data.sub.X$Richness)
class(Richness)
class(my.data.sub.$Richness)
class(my.data.sub$Richness)
my.data.sub.X <- my.data.sub.X[-c(my.data.sub.X$Glacier)]
my.data.sub.X <- my.data.sub.X[,-c(my.data.sub.X$Glacier)]
my.data.sub.X <- my.data.sub.X[,-c(14)]
View(my.data.sub.X)
my.data.sub <- as.data.frame(lapply(my.data.sub.X, function(x) as.numeric(as.character(x))))
my.data.sub.X <- rbind(my.data.sub, df)
my.data <- cbind(metadata, diversity)
#remove columns with data we're not interested in
my.data.sub <- subset(my.data, select = c(1, 10, 16, 19, 34, 36, 48, 50, 51, 52, 53, 54, 55, 56))
rownames(my.data.sub) <- my.data.sub$Run
my.data.sub <- subset(my.data.sub, select = -c(1))
my.data.sub <- as.data.frame(lapply(my.data.sub, function(x) as.numeric(as.character(x))))
my.data.sub$Glacier <- metadata$Glacier
my.data.sub <- my.data.sub[complete.cases(my.data.sub), ]
Chlorophyll <- sample(as.numeric(my.data.sub$Chlorophyll), size = 12)
Depth <- sample(as.numeric(my.data.sub$Depth), size = 12)
DNA_per_g <- sample(as.numeric(my.data.sub$DNA_per_g), size = 12)
org_matter <- sample(as.numeric(my.data.sub$org_matter), size = 12)
pH <- sample(as.numeric(my.data.sub$pH), size = 12)
water_content <- sample(as.numeric(my.data.sub$water_content), size = 12)
area <- sample(as.numeric(my.data.sub$area), size = 12)
Faith <- sample(as.numeric(my.data.sub$Faith), size = 12)
Richness <- sample(as.numeric(my.data.sub$Richness), size = 12)
Shannon <- sample(as.numeric(my.data.sub$Shannon), size = 12)
Simpson <- sample(as.numeric(my.data.sub$Simpson), size = 12)
Inv.Simpson <- sample(as.numeric(my.data.sub$Inv.Simpson), size = 12)
Pilou <- sample(as.numeric(my.data.sub$Pilou), size = 12)
Glacier <- rep("Solman", 12)
df <- cbind(Chlorophyll, Depth, DNA_per_g, org_matter, pH, water_content, area, Faith, Richness, Shannon, Simpson, Inv.Simpson, Pilou, Glacier)
my.data.sub.X <- rbind(my.data.sub, df)
my.data.sub.X.1 <- my.data.sub.X[,-c(14)] #remove glacier names
my.data.sub <- as.data.frame(lapply(my.data.sub.X.1, function(x) as.numeric(as.character(x))))
#re-add glacier names
my.data.sub$Glacier <- my.data.sub.X$Glacier
View(my.data.sub)
plot(Richness ~ Glacier, data = my.data.sub)
class(my.data.sub$Richness)
summary(my.data.sub)
#re-add glacier names
my.data.sub$Glacier <- as.factor(my.data.sub.X$Glacier)
plot(Richness ~ Glacier, data = my.data.sub)
#let's log richness and re-do our boxplot
my.data.sub$logRichness <- log(my.data.sub$Richness)
plot(logRichness ~ Glacier, data = my.data.sub)
seMean <- function(x){ # get standard error of the mean from a set of values (x)
x <- na.omit(x) # get rid of missing values
se <- sqrt(var(x)/length(x)) # calculate the standard error
return(se)  # tell the function to return the standard error
}
#Create function to calculate standard error of the mean
seMean <- function(x){ # get standard error of the mean from a set of values (x)
x <- na.omit(x) # get rid of missing values
se <- sqrt(var(x)/length(x)) # calculate the standard error
return(se)  # tell the function to return the standard error
}
glacierMeans <- tapply(my.data.sub$logRichness, my.data.sub$Glacier, FUN = mean, na.rm = TRUE)
print(glacierMeans)
print(glacierSE)
#Create function to calculate standard error of the mean
seMean <- function(x){ # get standard error of the mean from a set of values (x)
x <- na.omit(x) # get rid of missing values
se <- sqrt(var(x)/length(x)) # calculate the standard error
return(se)  # tell the function to return the standard error
}
#Get the glacier means
glacierMeans <- tapply(my.data.sub$logRichness, my.data.sub$Glacier, FUN = mean, na.rm = TRUE)
print(glacierMeans)
#get the standard error of the means
glacierSE <- tapply(my.data.sub$logRichness, my.data.sub$Glacier, FUN = seMean)
print(glacierSE)
#Let's plot these values
# get the upper and lower limits of the error bars
upperSE <- glacierMeans + glacierSE
lowerSE <- glacierMeans - glacierSE
# get a barplot
# - this function can report where the middle of the bars are on the x-axis
# - set the y axis limits to contain the error bars
barMids <- barplot(glacierMeans, ylim=c(0, max(upperSE)), ylab = 'log Richness')
# Now use the function to add error bars
# - draws arrows between the points (x0,y0) and (x1,y1)
# - arrow heads at each end (code=3) and at 90 degree angles
arrows(barMids, upperSE, barMids, lowerSE, ang=90, code=3)
library(gplot)
library(gplots)
# Get plots of group means and standard errors
plotmeans(logRichness ~ Glacier, data=my.data.sub, p=0.95, connect=FALSE)
glacierLM <- lm(logRichness ~ Glacier, data = my.data.sub)
anova(glacierLM)
summary(glacierLM)
anova(glacierLM)
summary(glacierLM)
plot(glacierLM)
par(mfrow=c(2,2), mar=c(5,5,1.5,1.5))
plot(glacierLM)
TukeyGlacier <- TukeyHSD(aov(glacierLM))
print(TukeyGlacier)
par(las=1, mar=c(4,10,3,1))
# las= 1 turns labels horizontal
# mar makes the left margin wider (bottom, left, top, right)
plot(TukeyGlacier)
Substrate <- rep(c("Ice", "Snow", "Cryoconite"), 33)
Substrate <- rep(c("Ice", "Snow", "Cryoconite"), 12)
Substrate <- c(Substrate, rep("Cryoconite", 4))
my.data.sub$Substrate <- as.factor(Substrate)
View(my.data.sub)
factorTable <- table(my.data.sub$Glacier, my.data.sub$Substrate)
print(factorTable)
chisq.test(factorTable) #if the p value is < 0.05 then we can conclude that the factors aren't independent,
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
# library(dplyr)
# library(tibble)
# library(ggpubr)
# library(phyloseq)
# library(ape)
library(vegan)
# library(ggrepel)
# install.packages("repr")
# library(repr) #for plotting our pairs of data
library("picante")
library(gplots)
load("results/cleaned_files/cleanedfiles_3.RData")
#hole area - calculate and add to dataframe
#Method as per Darcy et al (2018)
metadata$area <- pi*(as.numeric(metadata$diameter1)/2)*(as.numeric(metadata$diameter2)/2)
metadata$area <- metadata$area*10000
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
#set a seed so we get the same results each time we run this
set.seed(1961)
#rarefy the data to equal sampling depths
my.ps.rar <- rarefy_even_depth(my.ps)
#get relative abundances
my.ps.rar.rel <- transform_sample_counts(my.ps.rar, function(x) x/sum(x))
#Use vegan package to get shannon diversity index of each sample (remember to transpose the dataframe because it expects samples as rows and species as columns)
div_shan <- diversity(t(count_table_clean), index = "shannon")
#Use vegan package to get simpson diversity index of each sample
div_simp <- diversity(t(count_table_clean), index = "simpson")
#Use vegan package to get inverted simpson diversity index of each sample
div_inv_simp <- diversity(t(count_table_clean), index = "invsimpson")
#Faith's phylogenetic diversity
asv_tab <- as.data.frame(my.ps@otu_table) #Hellinger transformed asv table
my_tree <- my.ps@phy_tree #get my phylogenetic tree
#Check if the tree is rooted
my.ps@phy_tree #Rooted, includes branch lengths
#It is a rooted tree so...
df.pd <- pd(t(asv_tab), my_tree, include.root = TRUE) #find faiths diversity metric for each sample.
#Calculate Pilou evenness
div_pilou <- diversity(t(count_table_clean), index = "simpson")/log(df.pd$SR) #Here we use the species count we got earlier
#bind alpha diversity indices into a dataframe
diversity <- cbind(df.pd, div_shan, div_simp, div_inv_simp, div_pilou)
colnames(diversity) <- c("Faith", "Richness", "Shannon", "Simpson", "Inv Simpson", "Pilou")
my.data <- cbind(metadata, diversity)
#remove columns with data we're not interested in
my.data.sub <- subset(my.data, select = c(1, 10, 16, 19, 34, 36, 48, 50, 51, 52, 53, 54, 55, 56))
rownames(my.data.sub) <- my.data.sub$Run
my.data.sub <- subset(my.data.sub, select = -c(1))
my.data.sub <- as.data.frame(lapply(my.data.sub, function(x) as.numeric(as.character(x))))
my.data.sub$Glacier <- metadata$Glacier
my.data.sub <- my.data.sub[complete.cases(my.data.sub), ]
Chlorophyll <- sample(as.numeric(my.data.sub$Chlorophyll), size = 12)
Depth <- sample(as.numeric(my.data.sub$Depth), size = 12)
DNA_per_g <- sample(as.numeric(my.data.sub$DNA_per_g), size = 12)
org_matter <- sample(as.numeric(my.data.sub$org_matter), size = 12)
pH <- sample(as.numeric(my.data.sub$pH), size = 12)
water_content <- sample(as.numeric(my.data.sub$water_content), size = 12)
area <- sample(as.numeric(my.data.sub$area), size = 12)
Faith <- sample(as.numeric(my.data.sub$Faith), size = 12)
Richness <- sample(as.numeric(my.data.sub$Richness), size = 12)
Shannon <- sample(as.numeric(my.data.sub$Shannon), size = 12)
Simpson <- sample(as.numeric(my.data.sub$Simpson), size = 12)
Inv.Simpson <- sample(as.numeric(my.data.sub$Inv.Simpson), size = 12)
Pilou <- sample(as.numeric(my.data.sub$Pilou), size = 12)
Glacier <- rep("Solman", 12)
df <- cbind(Chlorophyll, Depth, DNA_per_g, org_matter, pH, water_content, area, Faith, Richness, Shannon, Simpson, Inv.Simpson, Pilou, Glacier)
my.data.sub.X <- rbind(my.data.sub, df)
my.data.sub.X.1 <- my.data.sub.X[,-c(14)] #remove glacier names
my.data.sub <- as.data.frame(lapply(my.data.sub.X.1, function(x) as.numeric(as.character(x))))
#re-add glacier names
my.data.sub$Glacier <- as.factor(my.data.sub.X$Glacier)
Substrate <- rep(c("Ice", "Snow", "Cryoconite"), 12)
Substrate <- c(Substrate, rep("Cryoconite", 4))
my.data.sub$Substrate <- as.factor(Substrate)
par(mfrow=c(1,2))
plot(Richness ~ Glacier, subset = Substrate, data = my.data.sub)
?plot
plot(Richness ~ Glacier, data = my.data.sub)
plot(Richness ~ Glacier, data = my.data.sub, subset = Substrate)
plot(Richness ~ Glacier, data = my.data.sub, subset = Substrate)
par(mfrow=c(1,2))
plot(Richness ~ Glacier, data = my.data.sub, subset = Substrate)
par(mfrow=c(1,2))
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier)
par(mfrow=c(1,2))
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Canada")
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Taylor")
par(mfrow=c(1,2))
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Canada", main = "Canada")
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Taylor", main = "Taylor")
library(lattice)
#we can do something similar with lattice plots
bwplot(Richness ~ Glacier | Substrate, data = my.data.sub)
par(mfrow=c(1,3))
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Canada", main = "Canada")
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Taylor", main = "Taylor")
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Solman", main = "Solman")
#we can do something similar with lattice plots
bwplot(Richness ~ Glacier | Substrate, data = my.data.sub)
par(mfrow=c(1,3))
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Canada", main = "Canada")
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Taylor", main = "Taylor")
plot(Richness ~ Substrate, data = my.data.sub, subset = Glacier == "Solman", main = "Solman")
#we can do something similar with lattice plots
bwplot(Richness ~ Substrate | Glacier, data = my.data.sub)
groups <- list(my.data.sub$Substrate, my.data.sub$Glacier)
groupMeans <- tapply(my.data.sub$Richness, groups, FUN = mean)
print(groupMeans)
seMean <- function(x){
# get rid of missing values
x <- na.omit(x)
# calculate the standard error
se <- sqrt(var(x)/length(x))
# tell the function to report the standard error
return(se)
}
groups <- list(my.data.sub$Substrate, my.data.sub$Glacier)
groupMeans <- tapply(my.data.sub$Richness, groups, FUN=mean)
print(groupMeans)
groupSE <- tapply(my.data.sub$Richness, groups, FUN=seMean)
print(groupSE)
# get upper and lower standard error height
upperSE <- groupMeans + groupSE
lowerSE <- groupMeans - groupSE
# create barplot
barMids <- barplot(groupMeans, ylim=c(0, max(upperSE)), beside=TRUE, ylab= ' log C value (pg) ' , col=c( ' white ' , ' grey70 '))
arrows(barMids, upperSE, barMids, lowerSE, ang=90, code=3, len=0.05)
# Get plots of group means and standard errors
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE)
# Get plots of group means and standard errors
par(mfrow=c(1,3))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Canada", p=0.95, connect=FALSE)
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Taylor", p=0.95, connect=FALSE)
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE)
# Get plots of group means and standard errors
par(mfrow=c(1,3))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Canada", p=0.95, connect=FALSE, main="Canada")
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Taylor", p=0.95, connect=FALSE)
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE)
# Get plots of group means and standard errors
par(mfrow=c(1,3))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Canada", p=0.95, connect=FALSE, main="Canada")
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Taylor", p=0.95, connect=FALSE, main = "Taylor")
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE, main = "Solman")
# Get plots of group means and standard errors
par(mfrow=c(1,3))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Canada", p=0.95, connect=FALSE, main="Canada", ylim=c(0, 700))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Taylor", p=0.95, connect=FALSE, main = "Taylor", ylim=c(0, 700))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE, main = "Solman", ylim=c(0, 700))
# Get plots of group means and standard errors
par(mfrow=c(1,3))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Canada", p=0.95, connect=FALSE, main="Canada", ylim=c(-1000, 700))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Taylor", p=0.95, connect=FALSE, main = "Taylor", ylim=c(-1000, 700))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE, main = "Solman", ylim=c(-1000, 700))
View(my.data.sub)
# Get plots of group means and standard errors
par(mfrow=c(1,3))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Canada", p=0.95, connect=FALSE, main="Canada", ylim=c(0, 700))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Taylor", p=0.95, connect=FALSE, main = "Taylor", ylim=c(0, 700))
plotmeans(Richness ~ Substrate, data=my.data.sub, subset = Glacier == "Solman", p=0.95, connect=FALSE, main = "Solman", ylim=c(0, 700))
model <- lm(Richness ~ Glacier + Substrate, data = my.data.sub)
model <- lm(Richness ~ Glacier + Substrate, data = my.data.sub)
#check diagnostic plot
par(mfrow=c(2,2))
plot(model)
#ANOVA of the model
anova(model)
#summary of the model
summary(model)
#ANOVA of the model
anova(model)
#summary of the model
summary(model)
#summary of the model
summary(model) #adjusted r = 0.03
# data frame of combinations of variables
gd <- rep(levels(my.data.sub$Substrate), times = 3)
print(gd)
tl <- rep(levels(mammals$Glacier), each = 3)
tl <- rep(levels(my.data.sub$Glacier), each = 3)
print(tl)
predVals <- data.frame(Substrate = gd, Glacier = tl)
predVals$predict <- predict(model, newdata = predVals)
print(predVals)
# data frame of combinations of variables
gd <- rep(levels(my.data.sub$Substrate), times = 3)
print(gd)
tl <- rep(levels(my.data.sub$Glacier), each = 3)
print(tl)
predVals <- data.frame(Substrate = gd, Glacier = tl)
predVals$predict <- predict(model, newdata = predVals)
print(predVals)
barMids <- barplot(groupMeans, ylim=c(0, 1.4), ylab='Richness', beside=TRUE, col=c('white', 'grey70'))
arrows(barMids, upperSE, barMids, lowerSE, ang=90, code=3, len=0.1)
points(barMids, predVals$predict, col='red', pch=12)
# data frame of combinations of variables
gd <- rep(levels(my.data.sub$Substrate), times = 3)
print(gd)
tl <- rep(levels(my.data.sub$Glacier), each = 3)
print(tl)
predVals <- data.frame(Substrate = gd, Glacier = tl)
predVals$predict <- predict(model, newdata = predVals)
print(predVals)
barMids <- barplot(groupMeans, ylim=c(0, 600), ylab='Richness', beside=TRUE, col=c('white', 'grey70'))
arrows(barMids, upperSE, barMids, lowerSE, ang=90, code=3, len=0.1)
points(barMids, predVals$predict, col='red', pch=12)
# data frame of combinations of variables
gd <- rep(levels(my.data.sub$Substrate), times = 3)
print(gd)
tl <- rep(levels(my.data.sub$Glacier), each = 3)
print(tl)
predVals <- data.frame(Substrate = gd, Glacier = tl)
predVals$predict <- predict(model, newdata = predVals)
print(predVals)
barMids <- barplot(groupMeans, ylim=c(0, 500), ylab='Richness', beside=TRUE, col=c('white', 'grey70'))
arrows(barMids, upperSE, barMids, lowerSE, ang=90, code=3, len=0.1)
points(barMids, predVals$predict, col='red', pch=12)
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
# library(dplyr)
# library(tibble)
# library(ggpubr)
# library(phyloseq)
# library(ape)
library(vegan)
# library(ggrepel)
# install.packages("repr")
# library(repr) #for plotting our pairs of data
library("picante")
library(gplots)
library(lattice)
rm(list=ls())
# library(dplyr)
# library(tibble)
# library(ggpubr)
# library(phyloseq)
# library(ape)
library(vegan)
# library(ggrepel)
# install.packages("repr")
# library(repr) #for plotting our pairs of data
library("picante")
library(gplots)
library(lattice)
load("results/cleaned_files/cleanedfiles_3.RData")
#hole area - calculate and add to dataframe
#Method as per Darcy et al (2018)
metadata$area <- pi*(as.numeric(metadata$diameter1)/2)*(as.numeric(metadata$diameter2)/2)
metadata$area <- metadata$area*10000
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
#set a seed so we get the same results each time we run this
set.seed(1961)
#rarefy the data to equal sampling depths
my.ps.rar <- rarefy_even_depth(my.ps)
#get relative abundances
my.ps.rar.rel <- transform_sample_counts(my.ps.rar, function(x) x/sum(x))
#Use vegan package to get shannon diversity index of each sample (remember to transpose the dataframe because it expects samples as rows and species as columns)
div_shan <- diversity(t(count_table_clean), index = "shannon")
#Use vegan package to get simpson diversity index of each sample
div_simp <- diversity(t(count_table_clean), index = "simpson")
#Use vegan package to get inverted simpson diversity index of each sample
div_inv_simp <- diversity(t(count_table_clean), index = "invsimpson")
#Faith's phylogenetic diversity
asv_tab <- as.data.frame(my.ps@otu_table) #Hellinger transformed asv table
my_tree <- my.ps@phy_tree #get my phylogenetic tree
#Check if the tree is rooted
my.ps@phy_tree #Rooted, includes branch lengths
#It is a rooted tree so...
df.pd <- pd(t(asv_tab), my_tree, include.root = TRUE) #find faiths diversity metric for each sample.
#Calculate Pilou evenness
div_pilou <- diversity(t(count_table_clean), index = "simpson")/log(df.pd$SR) #Here we use the species count we got earlier
#bind alpha diversity indices into a dataframe
diversity <- cbind(df.pd, div_shan, div_simp, div_inv_simp, div_pilou)
colnames(diversity) <- c("Faith", "Richness", "Shannon", "Simpson", "Inv Simpson", "Pilou")
my.data <- cbind(metadata, diversity)
#remove columns with data we're not interested in
my.data.sub <- subset(my.data, select = c(1, 10, 16, 19, 34, 36, 48, 50, 51, 52, 53, 54, 55, 56))
rownames(my.data.sub) <- my.data.sub$Run
my.data.sub <- subset(my.data.sub, select = -c(1))
my.data.sub <- as.data.frame(lapply(my.data.sub, function(x) as.numeric(as.character(x))))
my.data.sub$Glacier <- metadata$Glacier
my.data.sub <- my.data.sub[complete.cases(my.data.sub), ]
Substrate <- rep(c("Ice", "Snow", "Cryoconite"), 12)
Substrate <- c(Substrate, rep("Cryoconite", 4))
my.data.sub$Substrate <- as.factor(Substrate)
Substrate <- rep(c("Ice", "Snow", "Cryoconite"), 9)
Substrate <- c(Substrate, rep("Cryoconite", 1))
my.data.sub$Substrate <- as.factor(Substrate)
View(my.data.sub)
model <- lm(Richness ~ Glacier * Substrate, data = my.data.sub)
model <- lm(Richness ~ Glacier * Substrate, data = my.data.sub)
par(mfrow=c(2,2), mar=c(3,3,1,1), mgp=c(2,0.8,0))
plot(model)
#ANOVA
anova(model)
#SUMMARY
summary(model)
#ANOVA
anova(model) #glacier as a main effect seems like the only singificant predictor
#SUMMARY
summary(model)
my.data.sub$logRich <- log(my.data.sub$Richness)
my.data.sub$logArea <- log(my.data.sub$area)
model_1 <- lm(logRich ~ logArea * Glacier, data = my.data.sub)
summary(model_1)
anova(model_1)
summary(model_1)
#get the range of the data
rng <- range(my.data.sub$logArea)
#get a sequence from the min to the max with 100 equally spaced values:
LogAreaForFitting <- seq(rng[1], rng[2], length = 100)
print(LogAreaForFitting)
#get the range of the data
rng <- range(my.data.sub$logArea)
#get a sequence from the min to the max with 100 equally spaced values:
LogAreaForFitting <- seq(rng[1], rng[2], length = 100)
print(LogAreaForFitting)
#we're going to use these with the model to predict values of richness at each of those hole areas for each of our glaciers
#get a data frame of new data for the order
CanVals <- data.frame(logArea = LogAreaForFitting, Glacier = "Canada")
#get the predictions and standard error
CanPred <- predict(model_1, newdata = CanVals, se.fit = TRUE)
#get a data frame of new data for the order
TayVals <- data.frame(logArea = LogAreaForFitting, Glacier = "Taylor")
#get the predictions and standard error
TayPred <- predict(model_1, newdata = TayVals, se.fit = TRUE)
# plot the scatterplot of the data
plot(logRich ~ logArea, data = my.data.sub, col = Glacier)
plot(logRich ~ logArea, data = my.data.sub)
plot(logRich ~ logArea, data = my.data.sub, colour=Glacier)
plot(logRich ~ logArea, data = my.data.sub, col=Glacier)
class(my.data.sub$Glacier)
plot(logRich ~ logArea, data = my.data.sub, col=as.factor(Glacier))
#get the range of the data
rng <- range(my.data.sub$logArea)
#get a sequence from the min to the max with 100 equally spaced values:
LogAreaForFitting <- seq(rng[1], rng[2], length = 100)
print(LogAreaForFitting)
#we're going to use these with the model to predict values of richness at each of those hole areas for each of our glaciers
#get a data frame of new data for the order
CanVals <- data.frame(logArea = LogAreaForFitting, Glacier = "Canada")
#get the predictions and standard error
CanPred <- predict(model_1, newdata = CanVals, se.fit = TRUE)
#get a data frame of new data for the order
TayVals <- data.frame(logArea = LogAreaForFitting, Glacier = "Taylor")
#get the predictions and standard error
TayPred <- predict(model_1, newdata = TayVals, se.fit = TRUE)
# plot the scatterplot of the data
plot(logRich ~ logArea, data = my.data.sub, col=as.factor(Glacier))
# add the predicted lines
lines(CanPred$fit ~ LogAreaForFitting, col = "black")
lines(CanPred$fit + CanPred$se.fit ~ LogAreaForFitting, col = "black", lty = 2)
lines(CanPred$fit - CanPred$se.fit ~ LogAreaForFitting, col = "black", lty = 2)
lines(TayPred$fit ~ LogAreaForFitting, col = "red")
lines(TayPred$fit + TayPred$se.fit ~ LogAreaForFitting, col = "red", lty = 2)
lines(TayPred$fit - TayPred$se.fit ~ LogAreaForFitting, col = "red", lty = 2)
