log= signif(sum(resid(LogFit)^2)/(n-2 * 3), 3),
)
n <- length(Data2Fit$Richness)
list(lin=signif(sum(resid(LinFit2)^2)/(n-2 * 2), 3),
pow= signif(sum(resid(PowFit)^2)/(n-2 * 3), 3),
qua= signif(sum(resid(QuaFit)^2)/(n-2 * 3), 3),
vb= signif(sum(resid(VonFit)^2)/(n-2 * 3), 3),
log= signif(sum(resid(LogFit)^2)/(n-2 * 3), 3)
)
knitr::opts_chunk$set(echo = TRUE)
t <- seq(0, 22, 2)
N <- c(32500, 33000, 38000, 105000, 445000, 1430000, 3020000, 4720000, 5670000, 5870000, 5930000, 5940000)
set.seed(1234) # set seed to ensure you always get the same random sequence
data <- data.frame(t, N * (1 + rnorm(length(t), sd = 0.1))) # add some random error
names(data) <- c("Time", "N")
rm(list=ls())
t <- seq(0, 22, 2)
N <- c(32500, 33000, 38000, 105000, 445000, 1430000, 3020000, 4720000, 5670000, 5870000, 5930000, 5940000)
set.seed(1234) # set seed to ensure you always get the same random sequence
data <- data.frame(t, N * (1 + rnorm(length(t), sd = 0.1))) # add some random error
names(data) <- c("Time", "N")
ggplot(data, aes(x = Time, y = N)) +
geom_point(size = 3) +
labs(x = "Time (Hours)", y = "Population size (cells)")
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
t <- seq(0, 22, 2)
N <- c(32500, 33000, 38000, 105000, 445000, 1430000, 3020000, 4720000, 5670000, 5870000, 5930000, 5940000)
set.seed(1234) # set seed to ensure you always get the same random sequence
data <- data.frame(t, N * (1 + rnorm(length(t), sd = 0.1))) # add some random error
names(data) <- c("Time", "N")
ggplot(data, aes(x = Time, y = N)) +
geom_point(size = 3) +
labs(x = "Time (Hours)", y = "Population size (cells)")
library(ggplot2)
t <- seq(0, 22, 2)
N <- c(32500, 33000, 38000, 105000, 445000, 1430000, 3020000, 4720000, 5670000, 5870000, 5930000, 5940000)
set.seed(1234) # set seed to ensure you always get the same random sequence
data <- data.frame(t, N * (1 + rnorm(length(t), sd = 0.1))) # add some random error
names(data) <- c("Time", "N")
ggplot(data, aes(x = Time, y = N)) +
geom_point(size = 3) +
labs(x = "Time (Hours)", y = "Population size (cells)")
data$LogN <- log(data$N)
# visualise
ggplot(data, aes(x = t, y = LogN)) +
geom_point(size = 3) +
labs(x = "Time (Hours)", y = "log(cell number)")
(data[data$Time == 10,]$LogN - data[data$Time == 6,]$LogN)/(10-6)
#Alternatively, we can pick the maximum observed gradient of the curve using the diff() function
diff(data$LogN)
#so if we look at the maximum different between any pair of time points
max(diff(data$LogN))
#and divide this by two because this is the growth rate between two time points
max(diff(data$LogN))/2
lm_growth <- lm(LogN ~ Time, data = data[data$Time > 2 & data$Time <12, ])
#fit a linear model to log transformed data, by only for population size where time is greater than 2 and less than 12
summary(lm_growth)
logistic_model <- function(t, r_max, K, N_0){ # The classic logistic equation
return(N_0 * K * exp(r_max * t)/(K + N_0 * (exp(r_max * t) - 1)))
}
#First we need starting parameters
N_0_start <- min(data$N) #initial population size
K_start <- max(data$N) #maximum population size
r_max_start <- coef(lm_growth)["Time"] #Use our estimate from the OLS fitting
r_max_start
as.numeric(r_max_start)
#First we need starting parameters
N_0_start <- min(data$N) #initial population size
K_start <- max(data$N) #maximum population size
r_max_start <- as.numeric(coef(lm_growth)["Time"]) #Use our estimate from the OLS fitting
fit_logistic <- nlsLM(N ~ logistic_model(t = Time, r_max, K, N_0), data, list(r_max=r_max_start, N_0=N_0_start, K=K_start))
library(minpack.lm)
fit_logistic <- nlsLM(N ~ logistic_model(t = Time, r_max, K, N_0), data, list(r_max=r_max_start, N_0=N_0_start, K=K_start))
summary(fit_logistic)
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
# library(dplyr)
# library(tibble)
# library(ggpubr)
# library(phyloseq)
# library(ape)
library(vegan)
# library(ggrepel)
# install.packages("repr")
# library(repr) #for plotting our pairs of data
library(picante)
#library(gplots)
#library(lattice)
library(ggplot2)
library(minpack.lm)
load("results/cleaned_files/cleanedfiles_3.RData")
#hole area - calculate and add to dataframe
#Method as per Darcy et al (2018)
metadata$area <- pi*(as.numeric(metadata$diameter1)/2)*(as.numeric(metadata$diameter2)/2)
metadata$area <- metadata$area*10000
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
#Use vegan package to get shannon diversity index of each sample (remember to transpose the dataframe because it expects samples as rows and species as columns)
div_shan <- diversity(t(count_table_clean), index = "shannon")
#Use vegan package to get simpson diversity index of each sample
div_simp <- diversity(t(count_table_clean), index = "simpson")
#Use vegan package to get inverted simpson diversity index of each sample
div_inv_simp <- diversity(t(count_table_clean), index = "invsimpson")
#Faith's phylogenetic diversity
asv_tab <- as.data.frame(my.ps@otu_table) #Hellinger transformed asv table
my_tree <- my.ps@phy_tree #get my phylogenetic tree
#Check if the tree is rooted
my.ps@phy_tree #Rooted, includes branch lengths
#It is a rooted tree so...
df.pd <- pd(t(asv_tab), my_tree, include.root = TRUE) #find faiths diversity metric for each sample.
#Calculate Pilou evenness
div_pilou <- diversity(t(count_table_clean), index = "simpson")/log(df.pd$SR) #Here we use the species count we got earlier
#bind alpha diversity indices into a dataframe
diversity <- cbind(df.pd, div_shan, div_simp, div_inv_simp, div_pilou)
colnames(diversity) <- c("Faith", "Richness", "Shannon", "Simpson", "Inv Simpson", "Pilou")
my.data <- cbind(metadata, diversity)
#remove columns with data we're not interested in
my.data.sub <- subset(my.data, select = c(1, 10, 16, 19, 34, 36, 48, 50, 51, 52, 53, 54, 55, 56))
rownames(my.data.sub) <- my.data.sub$Run
my.data.sub <- subset(my.data.sub, select = -c(1))
my.data.sub <- as.data.frame(lapply(my.data.sub, function(x) as.numeric(as.character(x))))
my.data.sub$Glacier <- metadata$Glacier
my.data.sub <- my.data.sub[complete.cases(my.data.sub), ]
#get logs of the continuous variables we are interested in
my.data.sub$logChlo <- log(my.data.sub$Chlorophyll)
my.data.sub$logDepth <- log(my.data.sub$Depth)
my.data.sub$logDNA <- log(my.data.sub$DNA_per_g)
my.data.sub$logOrg <- log(my.data.sub$org_matter)
my.data.sub$logpH <- log(my.data.sub$pH)
my.data.sub$logArea <- log(my.data.sub$area)
my.data.sub$logRich <- log(my.data.sub$Richness)
my.data.sub$logWat <- log(my.data.sub$water_content)
#Straight Line Model
lineMod <- function(x,a,b){
return(a + b*x)
}
#Power Law Model
powMod <- function(x, a, b) {
return(a * x^b)
}
#Quadratic curve
quaMod <- function(x, a, b, c){
return(a + b*x + c*x^2)
}
#Von Bertalanffy
vonbert.w <- function(t, Winf, c, K){
Winf * (1 - exp(-K * t) + c * exp(-K * t))^3
}
#Logistic
logistic1 <- function(t, r, K, N0){
N0 * K * exp(r * t)/(K+N0 * (exp(r * t)-1))
}
#Get starting parameters for the logistic equation
ggplot(Data2Fit, aes(x = area, y = log(Richness))) +
geom_point(size = 3) +
labs(x = "Area", y = "log(richness)")
#Straight Line Model
lineMod <- function(x,a,b){
return(a + b*x)
}
#Power Law Model
powMod <- function(x, a, b) {
return(a * x^b)
}
#Quadratic curve
quaMod <- function(x, a, b, c){
return(a + b*x + c*x^2)
}
#Von Bertalanffy
vonbert.w <- function(t, Winf, c, K){
Winf * (1 - exp(-K * t) + c * exp(-K * t))^3
}
#Logistic
logistic1 <- function(t, r, K, N0){
N0 * K * exp(r * t)/(K+N0 * (exp(r * t)-1))
}
#subset our canada data
Data2Fit <- subset(my.data.sub, Glacier == "Canada")
#Get starting parameters for the logistic equation
ggplot(Data2Fit, aes(x = area, y = log(Richness))) +
geom_point(size = 3) +
labs(x = "Area", y = "log(richness)")
lm_rich <- lm(log(Richness) ~ area, data = Data2Fit[Data2Fit$area > 0 & Data2Fit$area <200, ])
summary(lm_rich)
r_start = as.numeric(coef(lm_rich)["area"])
K_start = max(Data2Fit$Richness)
N0_start = min(Data2Fit$Richness)
#We will scale the data before fitting to improve the stability of the estimates
#set a scale
scale <- 4000
#Straight Line Model
#LinFit <- nlsLM(Richness ~ lineMod(area, a, b), data = Data2Fit, start=list(a=.1, b=.1))
#we can achieve the same thing with a standard lm() fitting
LinFit2 <- lm(Richness/scale ~ area, data = Data2Fit)
#Power Model
PowFit <- nlsLM(Richness/scale ~ powMod(area, a, b), data = Data2Fit, start=list(a=.1, b=.1))
#Quadratic Curve
# QuaFit <- nlsLM(Richness ~ quaMod(area, a, b, c), data = Data2Fit, start=list(a=.1, b=.1, c=.1))
#we can achieve the same thing with the poly command
QuaFit <- lm(Richness/scale ~ poly(area,2), data = Data2Fit)
#Von Bertalanffy Model - this model isn't working so we'll leave it for now
VonFit <- nlsLM(Richness/scale ~ vonbert.w(area, Winf, c, K), start=list(Winf=0.75, c=0.01, K=0.01), data=Data2Fit)
#Logistic Model
LogFit <- nlsLM(Richness ~logistic1(area, r, K, N0), start=list(K=K_start, r=r_start, N0=N0_start), data=Data2Fit)
#get areas for plotting
Areas <- seq(min(Data2Fit$area), max(Data2Fit$area), len=200)
#get points for plotting
#Straight Line
Predic2PlotLin <- predict.lm(LinFit2, data.frame(area = Areas)) * scale
#Power Model
#Predic2PlotPow <- predict.lm(PowFit, data.frame(area = Areas))
Predic2PlotPow <- powMod(Areas, coef(PowFit)["a"],coef(PowFit)["b"]) *scale
#Quadratic Model
Predic2PlotQua <- predict.lm(QuaFit, data.frame(area = Areas)) * scale
#Von Bertalanffy Model
Predic2PlotVon <- predict(VonFit, newdata = list(area=Areas)) * scale
#Logistic Model
# Predic2PlotLog <- predict.lm(LogFit, data.frame(area = Areas))
Predic2PlotLog <- predict(LogFit, newdata = list(area=Areas)) #*scale
plot(Data2Fit$area, Data2Fit$Richness)
lines(Areas, Predic2PlotLin, col="blue", lwd=2.5)
lines(Areas, Predic2PlotPow, col="red", lwd=2.5)
lines(Areas, Predic2PlotQua, col="pink", lwd=2.5)
lines(Areas, Predic2PlotVon, col="orange", lwd=2.5)
lines(Areas, Predic2PlotLog, col="green", lwd=2.5)
legend("topleft", legend = c("linear", "power", "quadratic", "Von Bert", "logistic"), lwd=2, lty=1, col=c("blue", "red", "pink", "orange", "green"))
#subset our canada data
Data2Fit <- subset(my.data.sub, Glacier == "Canada")
#Get starting parameters for the logistic equation
ggplot(Data2Fit, aes(x = area, y = log(Richness))) +
geom_point(size = 3) +
labs(x = "Area", y = "log(richness)")
lm_rich <- lm(log(Richness) ~ area, data = Data2Fit[Data2Fit$area > 0 & Data2Fit$area <200, ])
summary(lm_rich)
r_start = as.numeric(coef(lm_rich)["area"])
K_start = max(Data2Fit$Richness)
N0_start = min(Data2Fit$Richness)
#We will scale the data before fitting to improve the stability of the estimates
#set a scale
scale <- 4000
#Straight Line Model
#LinFit <- nlsLM(Richness ~ lineMod(area, a, b), data = Data2Fit, start=list(a=.1, b=.1))
#we can achieve the same thing with a standard lm() fitting
LinFit2 <- lm(Richness/scale ~ area, data = Data2Fit)
#Power Model
PowFit <- nlsLM(Richness/scale ~ powMod(area, a, b), data = Data2Fit, start=list(a=.1, b=.1))
#Quadratic Curve
# QuaFit <- nlsLM(Richness ~ quaMod(area, a, b, c), data = Data2Fit, start=list(a=.1, b=.1, c=.1))
#we can achieve the same thing with the poly command
QuaFit <- lm(Richness/scale ~ poly(area,2), data = Data2Fit)
#Von Bertalanffy Model - this model isn't working so we'll leave it for now
VonFit <- nlsLM(Richness/scale ~ vonbert.w(area, Winf, c, K), start=list(Winf=0.75, c=0.01, K=0.01), data=Data2Fit)
#Logistic Model
LogFit <- nlsLM(Richness/scale ~logistic1(area, r, K, N0), start=list(K=K_start, r=r_start, N0=N0_start), data=Data2Fit)
#get areas for plotting
Areas <- seq(min(Data2Fit$area), max(Data2Fit$area), len=200)
#get points for plotting
#Straight Line
Predic2PlotLin <- predict.lm(LinFit2, data.frame(area = Areas)) * scale
#Power Model
#Predic2PlotPow <- predict.lm(PowFit, data.frame(area = Areas))
Predic2PlotPow <- powMod(Areas, coef(PowFit)["a"],coef(PowFit)["b"]) *scale
#Quadratic Model
Predic2PlotQua <- predict.lm(QuaFit, data.frame(area = Areas)) * scale
#Von Bertalanffy Model
Predic2PlotVon <- predict(VonFit, newdata = list(area=Areas)) * scale
#Logistic Model
# Predic2PlotLog <- predict.lm(LogFit, data.frame(area = Areas))
Predic2PlotLog <- predict(LogFit, newdata = list(area=Areas)) *scale
plot(Data2Fit$area, Data2Fit$Richness)
lines(Areas, Predic2PlotLin, col="blue", lwd=2.5)
lines(Areas, Predic2PlotPow, col="red", lwd=2.5)
lines(Areas, Predic2PlotQua, col="pink", lwd=2.5)
lines(Areas, Predic2PlotVon, col="orange", lwd=2.5)
lines(Areas, Predic2PlotLog, col="green", lwd=2.5)
legend("topleft", legend = c("linear", "power", "quadratic", "Von Bert", "logistic"), lwd=2, lty=1, col=c("blue", "red", "pink", "orange", "green"))
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
graphics.off()
set.seed(54321)
## 50 draws with each p
pp <- c(0.1, 0.5, 0.8)
N <- 20
reps <- 50
## histograms + density here
x <- seq(0, 50, by=1)
par(mfrow=c(1,3), bty="n")
View(x)
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
graphics.off()
load("results/cleaned_files/cleanedfiles_3.RData")
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
head(count_table_clean)
comm <- t(count_table_clean)
View(comm)
uni_sp = unique(row.names(comm))
uni_sp = unique(col.names(comm))
uni_sp = unique(colnames(comm))
sp_sum <- apply(comm, 2, sum)
site_sum <- apply(comm, 1, sum) #get the sum of each row (sample - how many individuals are there across all ASVs in the sample)
site_sum
uni_sp = unique(colnames(comm)) #get our unique species IDs
sp_sum <- apply(comm, 2, sum) #get the sum of each column (asv - how many individuals of the ASV are found across all samples)
site_sum <- apply(comm, 1, sum) #get the sum of each row (sample - how many individuals are there across all ASVs in the sample)
par(mfrow=c(2,2))
hist(sp_sum)
col=colorRamp(c('red', 'orange', 'blue'))
pc_cols = col(length(uni_sp))
plot(sp_sum[order(sp_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Sp Rank', ylab='Sum Cover')
hist(site_sum)
plot(site_sum[order(site_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Site Rank', ylab='Sum Cover')
uni_sp = unique(colnames(comm)) #get our unique species IDs
sp_sum <- apply(comm, 2, sum) #get the sum of each column (asv - how many individuals of the ASV are found across all samples)
site_sum <- apply(comm, 1, sum) #get the sum of each row (sample - how many individuals are there across all ASVs in the sample)
par(mfrow=c(2,2))
hist(sp_sum)
col=colorRamp(c('red', 'orange', 'blue'))
pc_cols = col(length(uni_sp))
plot(sp_sum[order(sp_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Sp Rank', ylab='Sum Cover')
hist(site_sum)
plot(site_sum[order(site_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Site Rank', ylab='Sum Cover')
View(metadata)
rm(list=ls())
graphics.off()
library(vegan)
load("results/cleaned_files/cleanedfiles_3.RData")
#hole area - calculate and add to dataframe
#Method as per Darcy et al (2018)
metadata$area <- pi*(as.numeric(metadata$diameter1)/2)*(as.numeric(metadata$diameter2)/2)
metadata$area <- metadata$area*10000
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
comm <- t(count_table_clean)
uni_sp = unique(colnames(comm)) #get our unique species IDs
sp_sum <- apply(comm, 2, sum) #get the sum of each column (asv - how many individuals of the ASV are found across all samples)
site_sum <- apply(comm, 1, sum) #get the sum of each row (sample - how many individuals are there across all ASVs in the sample)
par(mfrow=c(2,2))
hist(sp_sum) #histogram of total ASV abundances
col=colorRamp(c('red', 'orange', 'blue'))
pc_cols = col(length(uni_sp))
plot(sp_sum[order(sp_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Sp Rank', ylab='Sum Cover') #plot the total ASV abundances by order
hist(site_sum) #histogram of total abundances across all samples
plot(site_sum[order(site_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Site Rank', ylab='Sum Cover') #plot total abundances per sample by order
#We can see from the upper right hand graph that most species have low sum cover (total covers), so most species are rare
#These plots are not terribly helpful at understanding the variability in the community matrix so let’s get our enviornmental variables ready to use and start to conduct some actual analyses.
cols_to_keep = c('Chlorophyll', 'Depth', 'DNA_per_g', 'org_matter', 'pH', 'water_content', 'area')
env = aggregate(metadata[ , cols_to_keep], by = list(metadata$Run), function(x) x[1])
# aggregate does have a side effect of adding a column to the output which
# is the unique plotID in this case. We just need to move that so it is a
# row name instead.
row.names(env) = env[ , 1]
env = env[ , -1]
head(env)
all.equal(rownames(comm), rownames(env))
#Now we can rename the samples names to something simpler
rownames(comm) = 1:nrow(comm)
rownames(env) = 1:nrow(env)
View(comm)
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
graphics.off()
library(vegan)
load("results/cleaned_files/cleanedfiles_3.RData")
#hole area - calculate and add to dataframe
#Method as per Darcy et al (2018)
metadata$area <- pi*(as.numeric(metadata$diameter1)/2)*(as.numeric(metadata$diameter2)/2)
metadata$area <- metadata$area*10000
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
comm <- t(count_table_clean)
uni_sp = unique(colnames(comm)) #get our unique species IDs
sp_sum <- apply(comm, 2, sum) #get the sum of each column (asv - how many individuals of the ASV are found across all samples)
site_sum <- apply(comm, 1, sum) #get the sum of each row (sample - how many individuals are there across all ASVs in the sample)
par(mfrow=c(2,2))
hist(sp_sum) #histogram of total ASV abundances
col=colorRamp(c('red', 'orange', 'blue'))
pc_cols = col(length(uni_sp))
plot(sp_sum[order(sp_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Sp Rank', ylab='Sum Cover') #plot the total ASV abundances by order
hist(site_sum) #histogram of total abundances across all samples
plot(site_sum[order(site_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Site Rank', ylab='Sum Cover') #plot total abundances per sample by order
#We can see from the upper right hand graph that most species have low sum cover (total covers), so most species are rare
#These plots are not terribly helpful at understanding the variability in the community matrix so let’s get our enviornmental variables ready to use and start to conduct some actual analyses.
cols_to_keep = c('Chlorophyll', 'Depth', 'DNA_per_g', 'org_matter', 'pH', 'water_content', 'area')
env = aggregate(metadata[ , cols_to_keep], by = list(metadata$Run), function(x) x[1])
# aggregate does have a side effect of adding a column to the output which
# is the unique plotID in this case. We just need to move that so it is a
# row name instead.
row.names(env) = env[ , 1]
env = env[ , -1]
head(env)
#Let's check to make sure our explanatory matrix has the same sample names as our count table
all.equal(rownames(comm), rownames(env)) #TRUE!
#Now we can rename the samples names to something simpler
rownames(comm) = 1:nrow(comm)
rownames(env) = 1:nrow(env)
sample_pca = rda(comm, scale=TRUE)
sample_pca
#we can look at the structure of the list as so
str(sample_pca)
cryo_pca = rda(comm, scale=TRUE)
cryo_pca #this gives us a named list of results
#we can look at the structure of the list as so
str(cryo_pca)
#We can access each of the objects stored in a named list as we would reference columns in a dataframe using $
cryo_pca$
#We can access each of the objects stored in a named list as we would reference columns in a dataframe using $
cryo_pca$tot.chi
cryo_pca$eig
cryo_pca$AC$eig
cryo_pca$CA$eig
sum(cryo_pca$CA$eig)
#the ratio of the eigenvalue to the total variance is the amount of variance explained by each PCA axis
round(cryo_pca$CA$eig/cryo_pca$tot.chi, 2)
plot(cryo_pca)
biplot(cryo_pca)
cleanplot.pca(cryo_pca)
ordiplot(cryo_pca, display = 'sp')
orditorp(cryo_pca, display = 'sp')
ordiplot(cryo_pca, display = 'sp')
orditorp(cryo_pca, display = 'sp')
orditorp(cryo_pca, display = 'sp')
?orditorp
x <- ordiplot(cryo_pca, display = 'sp')
orditorp(x, display = 'sp')
?orditorp
cryo_ca = cca(comm)
cryo_dca = decorana(comm)
cryo_mds = metaMDS(comm)
plot(cryo_ca)
plot(cryo_dca)
plot(cryo_dca)
plot(cryo_mds)
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
graphics.off()
library(vegan)
load("results/cleaned_files/cleanedfiles_3.RData")
#hole area - calculate and add to dataframe
#Method as per Darcy et al (2018)
metadata$area <- pi*(as.numeric(metadata$diameter1)/2)*(as.numeric(metadata$diameter2)/2)
metadata$area <- metadata$area*10000
#Let's make our data into a phyloseq object
ASV = otu_table(data.frame(count_table_clean), taxa_are_rows = TRUE)
TAX = tax_table(as.matrix(taxonomy))
META	=	sample_data(data.frame(metadata, row.names = metadata$Run))
my.ps <- phyloseq(ASV, TAX, META, tree_clean)
comm <- t(count_table_clean)
uni_sp = unique(colnames(comm)) #get our unique species IDs
sp_sum <- apply(comm, 2, sum) #get the sum of each column (asv - how many individuals of the ASV are found across all samples)
site_sum <- apply(comm, 1, sum) #get the sum of each row (sample - how many individuals are there across all ASVs in the sample)
par(mfrow=c(2,2))
hist(sp_sum) #histogram of total ASV abundances
col=colorRamp(c('red', 'orange', 'blue'))
pc_cols = col(length(uni_sp))
plot(sp_sum[order(sp_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Sp Rank', ylab='Sum Cover') #plot the total ASV abundances by order
hist(site_sum) #histogram of total abundances across all samples
plot(site_sum[order(site_sum, decreasing=T)], type='o', col='red', lwd=2, xlab='Site Rank', ylab='Sum Cover') #plot total abundances per sample by order
#We can see from the upper right hand graph that most species have low sum cover (total covers), so most species are rare
#These plots are not terribly helpful at understanding the variability in the community matrix so let’s get our enviornmental variables ready to use and start to conduct some actual analyses.
cols_to_keep = c('Chlorophyll', 'Depth', 'DNA_per_g', 'org_matter', 'pH', 'water_content', 'area')
env = aggregate(metadata[ , cols_to_keep], by = list(metadata$Run), function(x) x[1])
# aggregate does have a side effect of adding a column to the output which
# is the unique plotID in this case. We just need to move that so it is a
# row name instead.
row.names(env) = env[ , 1]
env = env[ , -1]
head(env)
#Let's check to make sure our explanatory matrix has the same sample names as our count table
all.equal(rownames(comm), rownames(env)) #TRUE!
#Now we can rename the samples names to something simpler
rownames(comm) = 1:nrow(comm)
rownames(env) = 1:nrow(env)
rda_cryo = rda(comm, env)
nrow(comm)
nrow(env)
c1 <- complete.cases(comm)
View(c1)
c2 <- complete.cases(env)
View(c2)
c12 <- c1 & c2
View(c12)
comm_sub <- comm[c12, ]
env_sub <- env[c12, ]
rda_cryo = rda(comm_sub, env_sub)
cols_to_keep = c('Chlorophyll', 'Depth', 'DNA_per_g', 'org_matter', 'pH', 'water_content', 'area', 'Glacier')
env = aggregate(metadata[ , cols_to_keep], by = list(metadata$Run), function(x) x[1])
# aggregate does have a side effect of adding a column to the output which
# is the unique plotID in this case. We just need to move that so it is a
# row name instead.
row.names(env) = env[ , 1]
env = env[ , -1]
head(env)
#Let's check to make sure our explanatory matrix has the same sample names as our count table
all.equal(rownames(comm), rownames(env)) #TRUE!
#Now we can rename the samples names to something simpler
rownames(comm) = 1:nrow(comm)
rownames(env) = 1:nrow(env)
